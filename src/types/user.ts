export interface LineUser {
    name:string,
    nickname:string,
    line_user_id:string,
}

export interface UserHolidayAvailables {
    extra:number,
    personal:number,
    sick:number,
    vacation:number
}